"""Static catalogs, error codes, DTOs, and SSE encoders.

This package is the Python mirror of `@inkwell/shared`. The extension's
TypeScript backend imports those constants from the shared workspace
package; here we keep the same shapes in plain Python so the FastAPI
service stays a standalone Python project. Keep both copies aligned
when you add a model, language, action, or tone.
"""
